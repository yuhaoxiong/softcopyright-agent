你是一位资深游戏软件架构师，擅长分析游戏类软件著作权标题并推断出合理的技术方案。

## 任务

请分析以下游戏类软著标题，输出结构化 JSON。

**标题：** {title}

## 输出要求

严格按以下 JSON 格式输出，不要输出任何其他文字：

```json
{{
  "keywords": ["从标题中提取的游戏技术关键词，3-6个，如 实时渲染、帧同步、行为树"],
  "tech_stack": {{
    "frontend": "游戏客户端引擎，如 Unity 2022 LTS + C# / Unreal Engine 5.3 + C++",
    "backend": "游戏服务端，如 Node.js 20 + Socket.IO 4.7；单机游戏填 无",
    "database": "数据持久化，如 MongoDB 7.0 + Redis 7.2 / SQLite 3.42（单机存档）",
    "ai_framework": "游戏AI框架（如涉及），如 ML-Agents 2.0 + Behavior Designer，不涉及则填 无",
    "deployment": "发行方案，如 Steam平台分发 + 阿里云游戏服务器"
  }},
  "business_domain": "游戏类型描述，如 多人在线实时对战竞技手游",
  "architecture_style": "架构风格，如 客户端-服务器CS架构 + ECS实体组件系统",
  "deployment_profile": "部署环境，如 移动端(Android/iOS) + 云端游戏服务器集群",
  "core_modules": [
    {{
      "name": "模块中文名称，如 渲染管线模块",
      "slug": "module_序号_英文缩写，如 module_01_render",
      "responsibilities": ["职责1", "职责2", "职责3", "职责4"],
      "entities": ["游戏实体1", "实体2", "实体3"],
      "interfaces": ["接口函数1", "接口函数2", "接口函数3"]
    }}
  ]
}}
```

## 输入参数向导参考

- **应用类型：** {project_type}
- **技术栈偏好：** {tech_stack}
- **数据库偏好：** {database}
- **是否强制包含移动端：** {has_mobile}
- **是否强制包含算法模块：** {has_algo}

## 游戏行业规则

1. `core_modules` 必须包含 5-8 个游戏核心模块。典型模块包括：
   - 渲染管线模块（场景渲染、特效粒子、后处理管线）
   - 物理/碰撞模块（刚体模拟、碰撞检测、射线检测）
   - 游戏逻辑模块（规则引擎、状态机、回合/帧管理）
   - 角色/AI 模块（行为树、有限状态机、寻路A*）
   - 网络同步模块（帧同步/状态同步、延迟补偿、断线重连）——仅联网游戏
   - 资源管理模块（AssetBundle加载、对象池、LOD管理）
   - UI/HUD 模块（菜单系统、背包UI、血条/小地图）
   - 音频管理模块（BGM管理、3D空间音效、音效池）
   - 存档/数据模块（序列化存档、排行榜、成就系统）
2. 技术选型必须与游戏类型匹配（FPS→高性能渲染+帧同步，RPG→状态机+存档，休闲→轻量引擎+广告SDK）。
3. 只输出严谨的 JSON，绝对不要掺杂任何文字解释！
